# Postgres connection string (Railway injects this automatically in production).
# For the public Railway proxy add ?sslmode=require; local Postgres needs no SSL.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/glamour
